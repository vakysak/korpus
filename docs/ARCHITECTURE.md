# Korpus – architektura (v1)

Inspirace: PolyBoard (hardware libraries), Mozaik (joinery), MRPeasy (BOM verzování),
Cabinet Vision (placement), TruhlářPRO (cloud), Optimik (nářezový plán jako výstup).

První vrstva: katalog + šablony (materials, edges, suppliers, hardware, cabinet_templates, orders, BOM).
